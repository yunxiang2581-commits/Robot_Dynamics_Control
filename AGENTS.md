# AGENTS.md

## 项目目标
本仓库用于分步骤学习机器人运动学、动力学与控制，主要技术栈包括：
- Python
- Pinocchio
- MuJoCo
- NumPy / SciPy
- 后续会扩展到 IK、动力学项、WBC、优化控制

当前主学习路径为：

1. URDF 模型导入
2. FK（正运动学）
3. Jacobian
4. IK（逆运动学）
5. 动力学项
6. 控制原型

本项目的核心目标不是“尽快把代码写完”，而是：

- 理解每一步的原理
- 学会自己手动编写关键代码
- 使用 Codex 作为助教、审阅者、脚手架生成器
- 逐步建立从模型到控制的完整理解链条

---

## 工作方式
优先采用以下方式：

- 先解释原理和原因
- 先解释这一步为什么存在
- 明确这一步和主目标的关系
- 先生成带注释的代码骨架
- 保留 TODO，让用户手动实现关键逻辑
- 再进行小范围补全和审阅
- 脚本尽量保持简单、清晰、可运行

避免以下方式：

- 未经明确要求就重写整个文件
- 为了“更高级”而把代码写得过度抽象
- 用过于紧凑、难读的写法替换学习型代码
- 跳过当前基础任务，直接进入后续高级控制
- 静默修改变量名、文件结构、代码风格

---

## Codex 在本项目中的角色
Codex 应当被当作：

- 编程助教
- 骨架生成器
- 代码审阅者
- 小范围补丁助手
- 调试辅助工具

Codex 不应当被当作：

- 一次性整包代写工具
- 整个项目的自动重构器
- 不解释原理的代码倾倒器

推荐协作顺序：

1. 先解释原理、原因、目标
2. 生成带注释的脚本骨架
3. 让用户手动实现关键逻辑
4. 再由 Codex 进行补全或审阅
5. 最后验证结果，并解释结果含义

---

## 代码生成规则
在创建或修改代码时：

- 尽量保留现有文件结构
- 尽量保留现有变量名和函数名
- 学习脚本优先使用一个清晰的 `main()`
- 不要无故增加无关文件
- 不要过早抽象成复杂框架
- 除非明确要求，不要做大规模重构

对于学习任务：

- 优先生成“骨架 + TODO”，不要默认生成完整成品
- 若补全 TODO，必须解释每个 TODO 的作用
- 不要删除已有的教学性注释
- 代码风格应适合初学者阅读和修改

---

## 注释规则
所有学习脚本都应包含**充足的中文注释**。

对于每个主要代码块，注释应说明：

1. 这一部分要实现什么功能
2. 为什么这一步是必要的
3. 推荐使用哪个函数 / 方法 / API
4. 输入是什么
5. 输出是什么

对于每个 TODO，注释应说明：

1. 需要补什么内容
2. 推荐使用什么函数或方法
3. 完成后应得到什么结果

注释要求：

- 清楚
- 实用
- 面向学习
- 不要过度冗长
- 必须和当前任务直接相关

---

## 路径处理规则
“路径设置”是本仓库的正式学习内容之一。

编写脚本时：

- 不要依赖 shell 当前工作目录
- 优先通过 `__file__` 定位项目根目录
- 使用 `pathlib.Path`
- 对 URDF / 模型资源优先使用候选路径列表
- 自动创建输出目录
- 找不到文件时，抛出清晰错误

推荐写法：

- `PROJECT_ROOT = Path(__file__).resolve().parents[1]`
- `URDF_CANDIDATES = [...]`
- `OUT_DIR = PROJECT_ROOT / "outputs" / <任务名>`
- `OUT_DIR.mkdir(parents=True, exist_ok=True)`

若文件不存在：

- 明确列出检查过的路径
- 错误信息要足够清楚

---

## 模型搜索规则
“搜索目标模型对象”也是本仓库的正式学习内容之一。

在选择 frame 或 joint 时：

- 不要凭猜测直接写死名字
- 先做精确匹配
- 再做包含匹配
- 对重要目标使用候选列表

例如：

- 左脚 frame 候选列表
- 左膝 joint 候选列表
- pelvis / torso 候选列表

推荐逻辑：

1. 先精确匹配
2. 再做小写字符串包含匹配
3. 若没找到，则返回 None 或抛出明确错误

推荐学习并使用的 API：

- `model.names`
- `model.frames`
- `[f.name for f in model.frames]`
- `model.getFrameId(name)`
- `model.getJointId(name)`

---

## Pinocchio 学习规则
对于 Pinocchio 相关任务，必须显式说明学习目标。

在合适的时候，应解释这些核心概念：

- 固定基 vs 浮动基
- `nq` vs `nv`
- `pin.JointModelFreeFlyer()`
- `pin.neutral(model)`
- `forwardKinematics`
- `updateFramePlacements`
- `computeJointJacobians`
- `getFrameJacobian`
- `integrate`
- 后续再进入：
  - CRBA
  - RNEA
  - ABA

对于每个脚本，尽量说明：

- 输入状态是什么
- 查询的 frame / joint 是谁
- 当前在计算什么数学对象
- 这一步如何连接到主学习路径的下一步

---

## 当前项目约定
当前项目优先约定如下：

- 先使用 H1 模型
- 先关注 pelvis + 双腿 + 双脚
- 暂不把手臂作为主要分析对象
- 人形分析默认使用浮动基模型
- 第一版实现尽量最小化
- 所有任务优先输出文本结果，便于复盘

当前学习优先级：

1. 路径设置
2. 目标模型搜索
3. FK
4. Jacobian
5. Jacobian 验证
6. IK 目标定义
7. 仅位置 IK
8. 后续再进入动力学与控制

---

## 每个任务的脚本风格
每个新任务优先采用：

- 一个脚本
- 一个明确目标
- 一个输出文本文件
- 必要时加一个 note 文件

输出文件通常应包含：

- URDF 路径
- 选中的 frame / joint
- 关键维度（如 `nq`, `nv`）
- 当前任务的主要结果
- 必要的误差指标或解释

---

## 审阅规则
审阅代码时：

- 不要重写整个文件
- 先列问题
- 再给最小修改建议
- 对每个修改简单解释原因
- 保留学习型结构和注释

如果代码已经大体正确：

- 优先做小修小补
- 不要把易读代码改成更紧凑但更难学的代码
- 不要删除帮助学习的注释

---

## TODO 补全规则
如果用户要求 Codex 补 TODO：

- 只补当前请求的 TODO
- 补完后解释每个 TODO 的功能
- 解释每个 TODO 对应的原理
- 保持其他文件结构不变
- 不要悄悄扩展到后续任务

---

## 错误处理规则
当脚本可能失败时：

- 使用清晰异常
- 明确指出哪个文件 / frame / joint 没找到
- 必要时打印候选名称
- 不要静默失败

---

## 输出与验证规则
每个任务都应尽量产生“可验证结果”。

可接受的输出包括：

- `.txt` 摘要
- `.csv` 误差历史
- 图像 / 曲线
- 控制台日志

尽量包含：

- 选中的目标 frame / joint
- 关键维度与 shape
- 主要计算结果
- 对结果的简短解释

---

## 主学习原则
所有步骤都要回到主目标：

URDF -> FK -> Jacobian -> IK -> dynamics -> control

在写代码之前，应先说明：

- 这一步在算什么
- 为什么现在需要它
- 它支持后面的哪个任务

---

## 本仓库期望的 Codex 行为
好的行为：

- 生成骨架
- 增加高质量中文注释
- 保留学习型 TODO
- 尽量保留现有结构
- 温和审阅
- 解释原理

不好的行为：

- 未经要求直接全量重写
- 删除学习性注释
- 跳过当前基础步骤
- 在当前阶段还没理解时就直接跳去更高级控制

---

## Git / Docker / Safety Rules

在修改任何代码前，必须先运行：

```bash
git status
```

不要删除已有文件，除非用户明确要求。

涉及 IK / Jacobian / 控制器 / MuJoCo 可视化时，必须说明：

- 输入是什么
- 输出是什么
- 数学逻辑是否变化
- 改了哪些参数
- 可能风险是什么

修改完成后必须运行：

```bash
git diff --stat
```

不要自动执行：

```bash
git push
```

不要修改：

- `/var/run/docker.sock`
- Docker socket 权限

不要使用：

```bash
chmod 666 /var/run/docker.sock
```

如果 Docker 在 Codex 默认沙箱中被阻止：

- 请求用户授权在沙箱外运行
- 不要修改系统权限来绕过

---

## 如果 AGENTS.md 过长
若本文件变得过大，应只保留稳定工作规则。

具体任务细节应拆到单独文档，例如：

- `docs/task_4_ik_notes.md`
- `docs/review_checklist.md`
- `docs/pinocchio_learning_notes.md`

然后在本文件中引用这些文档，而不是无限扩充本文件。
