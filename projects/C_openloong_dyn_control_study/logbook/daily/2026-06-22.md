# 2026-06-22 学习日志

## 今日主题

继续推进 Project C 的 WSL + Docker 环境收尾，确认 D 盘上的虚拟化运行环境已经可用。

## 今日完成

### 1. 确认 WSL 发行版已运行

在 Windows 侧检查：

```text
wsl -l -v
```

结果：

```text
Ubuntu-22.04-ProjectC  Running  2
```

说明 D 盘上的 Ubuntu 22.04 WSL 发行版已经正常运行。

### 2. 确认 Docker 可用

在 WSL 内检查：

```text
docker version
docker info --format "DockerRootDir={{.DockerRootDir}}"
```

结果：

```text
Client=29.6.0
Server=29.6.0
DockerRootDir=/var/lib/docker
```

说明 Docker Engine 已在 WSL 内可用，数据根目录保留在 WSL 默认位置，由 D 盘上的发行版虚拟磁盘承载。

### 3. 确认项目镜像可构建

执行：

```text
bash projects/C_openloong_dyn_control_study/docker/openloong_ubuntu22_build/build_image.sh
```

结果：

```text
openloong-ubuntu22-build:local
```

镜像已成功构建，并命中缓存。

### 4. 确认脚本测试通过

执行：

```text
bash projects/C_openloong_dyn_control_study/tools/openloong_demo_runner/tests/test_openloong_watch_sync_build.sh
```

结果：通过。

## 今日关键理解

- WSL 发行版导入到 D 盘后，`/var/lib/docker` 仍然是合理的 DockerRootDir，因为它实际位于 D 盘上的 WSL 虚拟磁盘里。
- `openloong-ubuntu22-build:local` 已可直接复用，后续可以把重点放回学习链条本身。

## 下一步计划

进入最小 Pinocchio 复现：

```text
加载 floating-base URDF
输出 nq / nv
选定脚部 frame / joint
计算 J / M / h
生成 txt 复盘结果
```
